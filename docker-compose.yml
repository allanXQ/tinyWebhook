version: "3"
services:
  mongodb:
    container_name: mongodb
    image: mongo:latest
    ports:
      - 27010:27017
    volumes:
      - ./data:/data/db
    restart: always

  tinywebhook:
    container_name: node-api
    env_file:
      - .env
    build: .
    ports:
      - 3000:5000
